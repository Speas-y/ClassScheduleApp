# 项目结构分析

本文档用于说明本 Android 课表项目的目录结构、每个文件的职责，以及“接口、导入、提醒、数据流”等关键模块的位置。

## 一、项目总体结构

```text
课表App/
├── app/                         # Android 应用主模块，源码、资源、Manifest 和模块构建配置都在这里
│   ├── build.gradle             # app 模块 Gradle 配置：SDK、包名、依赖、Java 版本等
│   ├── proguard-rules.pro       # Release 混淆规则，目前仅保留默认占位
│   └── src/main/                # Android 主源码集
├── gradle/wrapper/              # Gradle Wrapper 配置目录
├── build.gradle                 # 根工程 Gradle 插件版本配置
├── settings.gradle              # Gradle 工程名称、仓库和 app 模块声明
├── gradle.properties            # Gradle/AndroidX 全局构建参数
├── gradlew                      # macOS/Linux 下的 Gradle Wrapper 启动脚本
├── README.md                    # 项目简介、功能、环境和依赖说明
├── 开发日志.md                  # 按日期记录功能、优化、修复等开发日志
└── 项目结构分析.md              # 当前文档
```

`.cursor/rules/` 是本地 Cursor 助手规则目录，已被 `.gitignore` 忽略，不进入仓库。其中：

- `dev-daily-log.mdc`：要求完成实质改动后维护 `开发日志.md`。
- `github-upload.mdc`：用户说“上传”时自动提交并推送 GitHub。
- `chinese-code-comments.mdc`：要求后续生成/修改代码时使用必要中文注释。

## 二、构建与配置文件

| 文件 | 作用 |
| --- | --- |
| `settings.gradle` | 配置插件仓库、依赖仓库、根工程名 `ClassScheduleApp`，并声明包含 `:app` 模块。 |
| `build.gradle` | 根工程 Gradle 文件，声明 Android Gradle Plugin 版本 `8.2.2`。 |
| `gradle.properties` | 配置 Gradle JVM 内存、UTF-8 编码、AndroidX、非传递 R 类。 |
| `gradle/wrapper/gradle-wrapper.properties` | Gradle Wrapper 版本与下载地址配置。 |
| `gradlew` | 通过固定 Gradle 版本执行构建命令，例如 `./gradlew assembleDebug`。 |
| `app/build.gradle` | app 模块构建配置：`applicationId`、`minSdk`、`targetSdk`、Java 17、ViewBinding、Room、Material、Lifecycle、Jsoup 等依赖。 |
| `app/proguard-rules.pro` | Release 混淆规则文件，目前没有额外规则。 |
| `.gitignore` | 忽略 Gradle/build、本地配置、IDE 文件和 `.cursor/` 本地规则。 |

## 三、AndroidManifest 与应用入口

| 文件 | 作用 |
| --- | --- |
| `app/src/main/AndroidManifest.xml` | 声明权限、Application、Activity、Receiver 和应用主题。 |

Manifest 中的关键声明：

- 权限：`INTERNET` 用于 WebView 导入教务网页；`POST_NOTIFICATIONS` 用于 Android 13+ 通知；`SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM` 用于课前精确提醒；`RECEIVE_BOOT_COMPLETED` 用于开机后恢复提醒；`WAKE_LOCK` 辅助闹钟唤醒。
- `ScheduleApplication`：应用进程入口，创建通知渠道。
- `SplashActivity`：启动入口，带启动动画。
- `MainActivity`：主界面容器，承载课表 Fragment 和底部导航。
- `AddCourseActivity`：添加/编辑课程页。
- `SettingsActivity`：设置页。
- `ImportActivity`：教务网页导入页。
- `CourseAlarmReceiver`：课前提醒闹钟触发接收器。
- `BootReceiver`：设备重启后重新登记课前提醒。

## 四、Java 源码结构

源码根包是 `app/src/main/java/com/schedule/app/`。

### 1. 根包 `com.schedule.app`

| 文件 | 作用 |
| --- | --- |
| `ScheduleApplication.java` | 应用级入口，创建 `course_reminder_channel` 通知渠道，供课前通知使用。 |
| `SplashActivity.java` | 启动页 Activity，播放 logo/文字进入动画，约 1.5 秒后进入 `MainActivity`。 |
| `MainActivity.java` | 主界面 Activity，初始化 `ScheduleFragment`，处理 Android 13+ 通知权限，配置底部导航和悬浮添加按钮。 |

### 2. 数据层 `data/`

#### `data/entity/`

| 文件 | 作用 |
| --- | --- |
| `Course.java` | Room 实体类，对应数据库表 `courses`。字段包括课程名、教师、地点、星期、节次、起止周、单双周类型、颜色。`isActiveInWeek()` 用于判断某课程在指定周是否生效。 |

#### `data/db/`

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `CourseDao.java` | **接口** | Room DAO 接口，定义课程 CRUD：查询全部课程、同步查询、按 id 查询、插入、批量插入、更新、删除、清空。它是项目里最明确的“数据库接口”。 |
| `AppDatabase.java` | 抽象类 | Room 数据库单例，注册 `Course` 实体，提供 `courseDao()`。数据库名是 `class_schedule_db`。 |

#### `data/repository/`

| 文件 | 作用 |
| --- | --- |
| `CourseRepository.java` | 数据仓库层，封装 `CourseDao`。UI 层通过它拿 `LiveData<List<Course>>`，写操作统一走单线程 Executor，避免主线程访问数据库。 |

### 3. 课表 UI `ui/schedule/`

| 文件 | 作用 |
| --- | --- |
| `ScheduleFragment.java` | 课表页 Fragment，绑定 `ScheduleViewModel` 和 `ScheduleView`。负责周次切换、日期栏更新、课程详情弹窗、编辑/删除入口。 |
| `ScheduleView.java` | 自定义课表 ViewGroup，负责绘制 7 列 × N 节课表网格、左侧节次时间，并把课程渲染成圆角卡片。 |
| `ScheduleViewModel.java` | 课表页状态管理，提供课程 LiveData、当前周 LiveData、上一周/下一周/设置周次逻辑，并根据学期开始日期计算当前教学周。 |

### 4. 添加/编辑课程 `ui/course/`

| 文件 | 作用 |
| --- | --- |
| `AddCourseActivity.java` | 添加或编辑课程页面。负责读取表单、校验课程名/星期/节次/周次、选择课程颜色、编辑时加载旧课程，最终通过 `CourseRepository` 插入或更新 `Course`。 |

### 5. 设置页 `ui/settings/`

| 文件 | 作用 |
| --- | --- |
| `SettingsActivity.java` | 设置页 Activity，使用自定义卡片式 UI。控制学期开始日期、每日节数、节次时间、课前提醒开关、Markdown 导入、教务网页导入、清空课程等功能。设置项写入 `SharedPreferences`，并在关键变更后重新调度课前提醒。 |

设置页控制的主要键：

- `semester_start_date`：学期开始日期，用于计算当前教学周和提醒日期。
- `total_sections`：每日节数，影响课表高度和节次配置。
- `section_数字_start` / `section_数字_end`：每节课自定义开始/结束时间。
- `notify_enabled`：是否开启课前提醒。
- `jwxt_url`：正方教务系统地址，由导入页读取。

### 6. 导入模块 `ui/import_/`

这是项目里“控制导入”的核心目录。

| 文件 | 作用 |
| --- | --- |
| `ImportActivity.java` | **教务网页导入入口**。内置 WebView 打开正方教务，用户登录并进入课表页后点击悬浮按钮导入。优先执行 JS 请求正方课表 JSON 接口，失败时再抓取当前 HTML 解析。导入页右上角也提供“用外部浏览器打开”和“从浏览器文件导入”，用于 WebView 加载慢时从浏览器保存的 HTML、JSON 或 Markdown 文件导入。导入前弹窗确认，确认后清空旧课程并批量写入新课程，同时重新调度提醒。 |
| `ZhengfangKbListJsonParser.java` | **正方 JSON 接口解析器**。解析 `xskbcx_cxXsgrkb.html` 返回的 `kbList` JSON，把课程名、教师、教室、星期、节次、周次转换成 `Course`。这是当前教务导入的优先解析方式。 |
| `ZhengfangParser.java` | **正方 HTML 兜底解析器**。当 JSON 接口获取失败时，解析当前课表 HTML 表格，尽量从表格单元格中提取课程信息。 |
| `ZhengfangWeekTextParser.java` | 周次文本解析工具。解析 `1-16周`、`1-16周(单)`、`3周` 等正方周次格式，输出起止周和单双周类型。 |
| `KbcxMarkdownParser.java` | **Markdown 导入解析器**。解析外部脚本生成的 `kbcx_schedule.md` 中“按星期 · 节次”表格。该入口由 `SettingsActivity` 的“从 Markdown 导入”控制。 |

导入链路总结：

```text
底部导航「导入」
  -> MainActivity 打开 ImportActivity
  -> WebView 登录/进入教务课表页
  -> 点击 fabImport
  -> JS_FETCH_KB_JSON 请求 xskbcx_cxXsgrkb.html
  -> ZhengfangKbListJsonParser 解析 JSON
  -> 如果 JSON 无结果，抓取 HTML
  -> ZhengfangParser 解析 HTML
  -> 弹窗确认
  -> CourseRepository.deleteAll()
  -> CourseRepository.insertAllAndCallback()
  -> AlarmScheduler.scheduleAllAlarms()
```

Markdown 导入链路：

```text
设置页「从 Markdown 导入」
  -> Android 文档选择器选择 .md/.txt
  -> KbcxMarkdownParser.parse()
  -> 弹窗确认
  -> 清空旧课程并写入新课程
  -> 重新调度课前提醒
```

外部浏览器兜底导入链路：

```text
导入页右上角「用外部浏览器打开」
  -> 系统浏览器登录教务并打开课表页
  -> 保存课表 HTML / 导出 JSON 或 Markdown
  -> 回到导入页选择「从浏览器文件导入」
  -> 依次尝试 KbcxMarkdownParser / ZhengfangKbListJsonParser / ZhengfangParser
  -> 弹窗确认
  -> 清空旧课程并写入新课程
  -> 重新调度课前提醒
```

### 7. 通知与闹钟 `notification/`

| 文件 | 作用 |
| --- | --- |
| `AlarmScheduler.java` | 课前提醒调度器。读取设置和课程数据，为当前周与下一周内尚未发生的课程注册上课前 10 分钟提醒；关闭提醒、无学期日期或日期异常时取消提醒。 |
| `CourseAlarmReceiver.java` | AlarmManager 触发的广播接收器，读取课程 id、课程名、地点、时间并调用 `NotificationHelper`。 |
| `NotificationHelper.java` | 构建并发送系统通知，点击通知回到 `MainActivity`。 |
| `BootReceiver.java` | 监听开机完成广播，重启后重新调用 `AlarmScheduler.scheduleAllAlarms()`，避免系统重启导致闹钟丢失。 |

提醒链路总结：

```text
设置学期开始日期 / 添加课程 / 导入课程 / 修改提醒设置
  -> AlarmScheduler.scheduleAllAlarms()
  -> 读取 CourseDao.getAllCoursesSync()
  -> 计算未来课程的提醒时间
  -> AlarmManager 注册 PendingIntent
  -> CourseAlarmReceiver.onReceive()
  -> NotificationHelper.showCourseReminder()
```

### 8. 工具类 `util/`

| 文件 | 作用 |
| --- | --- |
| `SectionTimeMapper.java` | 节次与上下课时间映射工具。提供默认 1～16 节时间，支持从设置读取每日节数和自定义节次时间。课表绘制、课程详情、提醒调度都依赖它。 |
| `ScheduleConstants.java` | 课表业务常量，目前主要是最大教学周 `MAX_TEACHING_WEEK = 30`。 |

## 五、资源目录结构

资源根目录是 `app/src/main/res/`。

### 1. 布局 `layout/`

| 文件 | 作用 |
| --- | --- |
| `activity_main.xml` | 主界面布局：Fragment 容器、右下角添加课程 FAB、底部导航栏。 |
| `fragment_schedule.xml` | 课表页布局：顶部标题/周次/周切换、星期日期栏、课表滚动容器。 |
| `activity_add_course.xml` | 添加/编辑课程页布局：卡片式课程信息、上课时间、重复设置、课程颜色、保存按钮。 |
| `activity_settings.xml` | 设置页布局：卡片式课程设置、提醒、数据管理等设置项。 |
| `activity_import.xml` | 教务导入页布局：Toolbar、WebView、页面加载进度条、导入 FAB。 |
| `activity_splash.xml` | 启动页布局：渐变背景、应用 logo、标题、标语。 |

### 2. 菜单 `menu/`

| 文件 | 作用 |
| --- | --- |
| `bottom_nav_menu.xml` | 主界面底部导航：课表、导入、设置。 |
| `import_menu.xml` | 导入页右上菜单：更换教务网址。 |
| `main_menu.xml` | 旧主界面 Toolbar 菜单资源，当前主界面已改为底部导航，主要保留历史兼容/备用。 |

### 3. 动画 `anim/`

| 文件 | 作用 |
| --- | --- |
| `splash_logo_enter.xml` | 启动页 logo 淡入放大动画。 |
| `splash_text_enter.xml` | 启动页标题/标语淡入上移动画。 |
| `splash_exit.xml` | 启动页退出淡出动画。 |
| `main_enter.xml` | 主界面进入淡入动画。 |

### 4. 颜色选择器 `color/`

| 文件 | 作用 |
| --- | --- |
| `bottom_nav_item.xml` | 底部导航图标和文字的选中/未选中颜色 selector。 |

### 5. XML 配置 `xml/`

| 文件 | 作用 |
| --- | --- |
| `preferences.xml` | 旧 Preference 设置页配置。当前设置页已改为自定义卡片布局，但这里仍记录原始设置项和键名，可作为设置结构参考。 |

### 6. 值资源 `values/` 与 `values-night/`

| 文件 | 作用 |
| --- | --- |
| `values/strings.xml` | 应用名称、底部导航文字、导入提示、错误文案等字符串。 |
| `values/colors.xml` | 主题色、浅色背景、文字色、课程色等颜色定义。 |
| `values/themes.xml` | 浅色主题 `Theme.ClassSchedule` 和启动页主题 `Theme.ClassSchedule.Splash`。 |
| `values/arrays.xml` | 每日节数选项：8～16 节。 |
| `values-night/themes.xml` | 夜间主题配置。 |

### 7. 图形资源 `drawable/`

背景与 UI 装饰：

| 文件 | 作用 |
| --- | --- |
| `bg_app.xml` | 应用浅灰背景。 |
| `bg_bottom_nav.xml` | 底部导航白色圆角背景。 |
| `bg_card.xml` | 通用白色圆角卡片背景。 |
| `bg_circle_soft.xml` | 淡蓝圆形背景，用于图标容器。 |
| `bg_day_selected.xml` | 课表日期栏“今天”蓝色高亮背景。 |
| `bg_input_card.xml` | 添加课程页输入/Spinner 的浅色圆角背景。 |
| `bg_week_pill.xml` | 顶部周次胶囊背景。 |
| `splash_background.xml` | 启动页渐变背景。 |
| `splash_logo_circle.xml` | 启动页 logo 白色圆形底。 |

图标与启动图：

| 文件 | 作用 |
| --- | --- |
| `ic_launcher_background.xml` | App 启动图标背景。 |
| `ic_launcher_foreground.xml` | App 启动图标前景。 |
| `ic_add.xml` | 添加课程图标。 |
| `ic_import.xml` | 导入图标。 |
| `ic_settings.xml` | 设置齿轮图标。 |
| `ic_today.xml` | 日历/今天图标。 |
| `ic_notification.xml` | 通知图标。 |
| `ic_chevron_left.xml` | 上一周/返回左箭头图标。 |
| `ic_chevron_right.xml` | 下一周/进入右箭头图标。 |
| `ic_schedule.xml` | 底部导航“课表”图标。 |
| `ic_compass.xml` | 底部导航“导入”图标。 |
| `ic_person.xml` | 底部导航“设置”图标，目前视觉上是人物图标，可后续换成更贴合设置的图标。 |

### 8. 启动图标 `mipmap-anydpi-v26/`

| 文件 | 作用 |
| --- | --- |
| `ic_launcher.xml` | Android 8+ 自适应启动图标配置，组合 foreground/background。 |

## 六、哪些是“接口”

按 Java/Android 语义区分：

- `CourseDao.java`：Room 数据库访问接口，是真正的 Java `interface`。
- `ScheduleView.OnCourseClickListener`：自定义 View 内部回调接口，用于课程卡片点击回调到 `ScheduleFragment`。
- `ImportActivity.JsBridge`：不是 Java interface，但它是 WebView JavaScript 与 Android 之间的桥接接口，通过 `@JavascriptInterface` 暴露 `onKbJson()` 和 `onHtmlReceived()` 给网页 JS 调用。
- 正方教务 JSON 接口：`ImportActivity` 中 JS 请求 `xskbcx_cxXsgrkb.html`，由 `ZhengfangKbListJsonParser` 解析。

## 七、哪些文件控制导入

导入相关文件集中在这些位置：

| 文件 | 导入职责 |
| --- | --- |
| `MainActivity.java` | 底部导航点击“导入”后打开 `ImportActivity`。 |
| `SettingsActivity.java` | 提供 Markdown 导入入口和教务网页导入入口。 |
| `ImportActivity.java` | 正方教务 WebView 导入主控制器。 |
| `ZhengfangKbListJsonParser.java` | 正方课表 JSON 解析。 |
| `ZhengfangParser.java` | 正方课表 HTML 兜底解析。 |
| `ZhengfangWeekTextParser.java` | 正方周次字段解析。 |
| `KbcxMarkdownParser.java` | Markdown 表格导入解析。 |
| `CourseRepository.java` | 导入确认后清空旧数据、批量写入新课程。 |
| `AlarmScheduler.java` | 导入完成后重新登记课前提醒。 |
| `activity_import.xml` | 导入页 WebView 和导入按钮布局。 |
| `import_menu.xml` | 导入页“更换教务网址”菜单。 |
| `strings.xml` | 导入确认、导入成功、解析失败等文案。 |

## 八、数据流与模块关系

### 1. 手动添加课程

```text
MainActivity 悬浮加号
  -> AddCourseActivity
  -> 用户填写课程信息
  -> new Course(...)
  -> CourseRepository.insert()/update()
  -> CourseDao 写入 Room
  -> ScheduleViewModel 观察 LiveData
  -> ScheduleFragment 刷新 ScheduleView
```

### 2. 展示课表

```text
CourseDao.getAllCourses()
  -> CourseRepository.getAllCourses()
  -> ScheduleViewModel.allCourses
  -> ScheduleFragment.refreshSchedule()
  -> ScheduleView.setCourses()
  -> 自绘网格 + 添加课程卡片 View
```

### 3. 计算当前周

```text
SettingsActivity 保存 semester_start_date
  -> ScheduleViewModel.calculateCurrentWeek()
  -> ScheduleConstants.MAX_TEACHING_WEEK 限制最大周
  -> ScheduleFragment 更新周次与日期栏
```

### 4. 课前提醒

```text
课程变更/设置变更/导入完成/开机完成
  -> AlarmScheduler.scheduleAllAlarms()
  -> SectionTimeMapper 读取节次时间
  -> Course.isActiveInWeek() 判断生效周
  -> AlarmManager 注册广播
  -> CourseAlarmReceiver
  -> NotificationHelper
```

## 九、后续维护建议

- 如果要继续美化“导入页”，优先修改 `activity_import.xml` 和 `ImportActivity.java`。
- 如果要替换底部“设置”图标，修改 `bottom_nav_menu.xml` 中 `nav_settings` 的图标，或新增更合适的 `drawable/ic_*.xml`。
- 如果要新增导入来源，应放在 `ui/import_/`，解析结果统一转换成 `List<Course>`，再通过 `CourseRepository` 写入。
- 如果要改课程数据字段，需要同步修改 `Course.java`、`CourseDao.java`、`AppDatabase` 版本和数据库迁移。
- 如果要改提醒策略，优先查看 `AlarmScheduler.java`，它是提醒时间计算和闹钟注册的中心。
