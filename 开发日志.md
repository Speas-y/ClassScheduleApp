# 开发日志

按日期记录功能新增、修改优化、重要修复等。**新日期写在最上面**。助手在完成实质改动时会**主动**更新本文，无需你先说「记日志」；你也可以随时说「记一下日志」用于补记或汇总。

**同步 GitHub**：在 Cursor 中打开本项目，对助手说 **「上传」**，会代为执行提交并推送到远程。写日志不会自动推送；需要备份远程时请说 **「上传」**。

---

## 2026-06-18

- **其他**：根据课程设计论文 Word 模板，为当前 Android 课表项目新增论文写作计划、分章节草稿、合并版 Markdown 和 Word 论文文档。
- **修复**：为导入页布局显式补充 CoordinatorLayout 依赖，解决布局预览器找不到 `androidx.coordinatorlayout.widget.CoordinatorLayout` 导致的 Render problem。
- **其他**：按要求回滚数据库改造，恢复 Room 数据库版本，并同步恢复课程设计论文中的数据库设计、实现、测试和参考文献内容。
- **优化**：导入页 WebView 改回默认移动端 User-Agent，并增加教务网页主页面加载失败提示，减少导入页打开慢或卡住时的误判。
- **优化**：导入页 WebView 使用标准移动 Chrome User-Agent，允许混合内容加载，并增加 15 秒加载慢提示，提升老教务系统页面兼容性。
- **功能**：导入页新增外部浏览器兜底导入方案，可用系统浏览器打开教务系统，并从浏览器保存的 HTML、JSON 或 Markdown 文件导入课表。

---

## 2026-06-11

- **其他**：新增 `项目结构分析.md`，梳理项目目录、各源码/资源文件职责，以及接口、导入、提醒和数据流关键链路。

---

## 2026-06-07

- **功能**：新增应用启动页进入动画，启动时展示课表图标、应用名和标语后淡入主界面。
- **优化**：重做主课表、添加课程和设置页 UI，改为浅色背景、圆角卡片、底部导航、悬浮添加按钮和低饱和课程色块风格。
- **优化**：将设置页从默认偏好列表改为自定义卡片分组界面，统一课表 UI 调性并保留原有设置功能。
- **其他**：新增中文代码注释规则，并为主导航、课表绘制、设置页和添加课程关键逻辑补充中文注释。

---

## 2026-03-26

- **其他**：将 `.cursor/` 加入 `.gitignore` 并从 Git 移除已跟踪的 Cursor 规则文件，规则仅保留本地，不再进入仓库。
- **其他**：为主要 Java 类补充中文 JavaDoc（Application、主界面、数据层、课表 UI、节次工具、通知与闹钟、添加/设置页面等），便于快速理解 MVVM 与导入/提醒流程。

---
