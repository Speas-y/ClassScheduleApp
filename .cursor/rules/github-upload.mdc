---
description: 用户在聊天中说「上传」时，代为 git 提交并推送到 GitHub
alwaysApply: true
---

# 「上传」→ 提交并推送 GitHub

当用户消息主要是 **「上传」**，或明确表达 **把当前改动同步/提交到 GitHub**（如「push」「同步到远程」）时：

1. **直接执行**，不要只列出「你可以运行这些命令」；用终端在项目根目录（本仓库）运行 git。
2. 先 `git status`；必要时 `git diff --stat` 便于写提交说明。
3. **无未暂存/未提交改动**：若已领先 `origin`，执行 `git push`；若完全同步，说明「没有可提交的更改，已与远程一致」。
4. **有改动**：`git add -A` → `git commit -m "..."` → `git push`。提交说明用简短中文；用户没写说明时，根据变更内容自拟一行摘要。
5. **不要**把明显机密强行提交；`local.properties` 等一般已被 `.gitignore` 忽略，若仍出现在 `status` 里，勿加入提交并提醒用户。
6. `commit` 或 `push` 失败时，贴出终端错误并说明可能原因（冲突、未配置远程、认证、网络等）。
