---
description: 维护开发日志.md，记录每日改动、优化与功能；注明 GitHub 需通过「上传」同步
alwaysApply: true
---

# 开发日志（`开发日志.md`）

## 文件位置

- 在本仓库**根目录**维护 **`开发日志.md`**。

## 何时更新

- **不必等用户说「记日志」**：本轮对话里只要完成了**实质性**改动（新功能、明显优化/重构、重要 bug 修复、构建或依赖变更等），助手应在**本次任务收尾前主动**打开并更新 `开发日志.md` 的当日条目；用户未提起也要做。
- 用户主动说 **「记日志」「更新开发日志」「今天改了什么记一下」** 时，同样必须整理写入；可用于**补记**漏写的、或**汇总**跨多轮对话的改动（可结合 `git diff` / `git status`）。
- **同一自然日**只保留一个 `## YYYY-MM-DD` 区块：多次改动并入同一日期下的列表，不要重复建同名日期标题。

## 写法

- **日期倒序**：新增日期时，把新的 `## YYYY-MM-DD` 放在文件中**所有日期标题的最上面**（最新在上）。
- 列表项建议带类型前缀，便于扫读：**功能** / **优化** / **修复** / **重构** / **其他**。
- 用**简短中文**，一条一事；避免大段粘贴代码。

## GitHub 同步（与日志的关系）

- **推送到远程**由独立规则处理：用户在聊天里说 **「上传」** 时，助手代为执行 `git add` → `commit` → `push`，详见 **`github-upload.mdc`**。
- 开发日志**只记录事实**，不代替提交；在重大改动写完后，可**顺带提醒**：若需备份到 GitHub，可对助手说 **「上传」**。

## 文件不存在时

- 若仓库里还没有 `开发日志.md`，先创建它：顶部用简短说明本文件用途，并**写明**「同步 GitHub：在 Cursor 中对本项目说 **上传**」及规则文件 `github-upload.mdc` 的存在。
